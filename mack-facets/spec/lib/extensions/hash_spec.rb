require 'pathname'
require Pathname(__FILE__).dirname.expand_path.parent.parent + 'spec_helper'

describe Hash do
  
  describe "-" do
    
    it "should destructively removed elements in the Hash with the specified keys"
    
  end
  
  describe "join" do
    
    it "should return a string that is formatted with the proper join syntax that's specified"
    
  end
  
  describe "to_params" do
    
    it "should convert the Hash to a string representing query string parameters"
    
    it "should escape the values "
    
  end
  
end